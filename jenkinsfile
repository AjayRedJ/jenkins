pipeline{
    agent{
        docker {image 'nginx:latest'}
    }
    stages{
        stage('check nginx version'){
            steps{
                sh 'nginx -version'
            }
        }
    }
}